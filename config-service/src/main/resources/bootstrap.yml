server:
  port: 8888

spring:
  application:
    name: config-service
  profiles:
    active: local,dev
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sbarbat/cloudshop-settings
